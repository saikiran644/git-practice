# git configure

configuring user name and mail id:
	-git config --global user.name "user_name"
	-git config --global user.email "user_email_id"
	-git config --global core.editor "command to open editor"
	
list the entered entries:
	-git config --global --list